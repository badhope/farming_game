# ===========================================
# 核心依赖
# ===========================================

# Web 后端
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # 表单数据处理

# 数据库
sqlalchemy>=2.0.0
aiosqlite>=0.19.0  # SQLite 异步支持

# 数据验证和序列化
pydantic>=2.0.0
pydantic-settings>=2.0.0
pydantic[email]>=2.0.0
email-validator>=2.0.0

# 认证
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# 监控
psutil>=5.9.0

# ===========================================
# AI 功能（可选，用于未来接入 LLM）
# ===========================================
# requests>=2.28.0  # 用于调用 OpenAI API
# openai>=0.27.0    # OpenAI SDK

# ===========================================
# 开发工具
# ===========================================
pytest>=7.0.0
pytest-asyncio>=0.21.0  # 异步测试支持
mypy>=0.900
black>=22.0.0
flake8>=4.0.0
httpx>=0.25.0  # API 测试

# ===========================================
# 部署工具
# ===========================================
# docker-compose  # Docker 容器化（通过系统安装）
